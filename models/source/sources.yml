version: 2

sources:
  - name: source
    database: dbt_tutorial_dev
    schema: source
    description: "Raw source schema for sales, returns, and supporting dimensions."
    tables:
      - name: sales
        description: "Raw sales transactions."
      - name: customer
        description: "Raw customer dimension."
      - name: product
        description: "Raw product dimension."
      - name: date
        description: "Raw calendar date dimension."
      - name: store
        description: "Raw store dimension."
      - name: returns
        description: "Raw return transactions."
